{
  "name": "volt-construction-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd .. && npm install && npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "multer": "^2.0.0"
  }
}
